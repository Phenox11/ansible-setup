- hosts: all
  become: true
  tasks:
    - name: update packages
      apt:
        update_cache = yes
    - name: install
      apt: name={{item}}
      with_items:
        - htop
        - zellij
        - eza
        - bat
      state = present
